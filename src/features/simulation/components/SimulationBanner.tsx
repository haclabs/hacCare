/**
 * ===========================================================================
 * SIMULATION BANNER
 * ===========================================================================
 * Banner component for simulation mode (currently disabled)
 * Removed red banner as it looked like an error/warning to students
 * ===========================================================================
 */

import React from 'react';

const SimulationBanner: React.FC = () => {
  // Banner removed - red color looked like an error to students
  // Simulation context is clear from the blue header and tenant switcher
  return null;
};

export default SimulationBanner;
