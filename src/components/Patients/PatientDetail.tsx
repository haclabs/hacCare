Here's the fixed version with all missing closing brackets added:

```typescript
};
```

The file was missing a single closing curly brace at the very end to close the `PatientDetail` component. The rest of the file's brackets were properly matched.